# Whisper Gateway URL — when running with docker-compose this defaults to
# http://whisper-gateway:5148 on the infra-net network.
# For local dev, point it at any reachable gateway.
WHISPER_GATEWAY_URL=http://100.79.146.102:5148
